{
  "name": "gamified-helpdesk-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",  // Assuming Vite, change to "react-scripts start" for CRA
    "build": "tsc && vite build", // Change to "react-scripts build" for CRA
    "serve": "vite preview", // Change to "serve -s build" for CRA
    "lint": "eslint . --ext .ts,.tsx",
    "test": "jest" // Or Vitest
  },
  "dependencies": {
    "@emotion/react": "^11.10.0",
    "@emotion/styled": "^11.10.0",
    "@mui/icons-material": "^5.10.0",
    "@mui/material": "^5.10.0",
    "@mui/x-data-grid": "^5.17.0", // For DataGrid
    "@mui/x-date-pickers": "^5.0.0", // For Date Pickers
    "axios": "^0.27.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.3.0"
    // Add other dependencies like state management (redux, zustand), charting libraries
  },
  "devDependencies": {
    "@types/react": "^18.0.17",
    "@types/react-dom": "^18.0.6",
    "@vitejs/plugin-react": "^2.0.1", // Assuming Vite
    "eslint": "^8.22.0",
    "jest": "^28.1.3", // Or Vitest
    "typescript": "^4.7.4",
    "vite": "^3.0.7" // Assuming Vite
    // Add other dev dependencies (e.g. @testing-library/react)
  }
}
